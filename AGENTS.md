# Repository Instructions

Before changing this backend:

1. Read `README.md` for runtime, deployment, and migration commands.
2. Keep API responses compatible with the frontend contract.
3. Preserve the D1 migration history and business error codes.
4. Run `uv run pytest` and a local Wrangler preview for runtime changes.
